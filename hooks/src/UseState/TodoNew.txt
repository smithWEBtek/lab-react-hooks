import React, { useState } from 'react';

const TodoNew = (props) => {
  const [newTodo, setNewTodo] = useState({ title: '', url: '' });

  const handleTitleChange = (event) => {
    event.preventDefault()
    const changedTitle = event.target.value
    setNewTodo({ title: changedTitle })
  }

  const saveTodo = (data) => {
    JSON.stringify(this)
    debugger;
  }

  const form = (
    <div>
      <h3>a new todo</h3>
      <form>
        {/* <input
          type="text"
          name="title"
          placeholder="title"
          onChange={() => handleTitleChange} /> */}
        {/* <input
          type="text"
          name="url"
          placeholder="url" /> */}
        {/* <input
          type="submit"
          onSubmit={() => saveTodo}>Save Todo</input> */}
      </form>
    </div>
  )

  return form;
}

export default TodoNew;